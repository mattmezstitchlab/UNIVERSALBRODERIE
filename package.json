{
  "name": "universalbroderie",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 --bind 0.0.0.0",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
